# =============================================================================
# Dashboard card - Licznik restartów Deco (TYLKO ODCZYT, bez przycisków)
# =============================================================================

# Użycie type: sensor zamiast type: entity — nie pokazuje przycisków
# Zwiększ/Zmniejsz/Reset
type: sensor
entity: counter.licznik_restartow_deco_przez_sonoff
name: "Liczba awarii Sonoff (restart Deco)"
icon: mdi:wifi-alert
grid_options:
  columns: 6
  rows: 2
